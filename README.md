# Continuous

This program aims to calculate continuous external forces applied to objects in orbit. It will be primarily based in the underlying vector fields and will hopefully output the Keplerian Elements after each time step, using RK4 integration, in a pertubationless, two-dimensional system with two bodies.
